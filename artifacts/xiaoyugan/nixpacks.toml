[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.build]
cmds = ["pnpm install --no-frozen-lockfile && pnpm --filter @workspace/xiaoyugan run build"]

[start]
cmd = "pnpm --filter @workspace/xiaoyugan run preview -- --host 0.0.0.0 --port $PORT"